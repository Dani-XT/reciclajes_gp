import { ParallaxOne } from './parallax-one';

describe('ParallaxOne', () => {
  it('should create an instance', () => {
    const directive = new ParallaxOne();
    expect(directive).toBeTruthy();
  });
});

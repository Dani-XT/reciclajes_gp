import { ParallaxTwo } from './parallax-two';

describe('ParallaxTwo', () => {
  it('should create an instance', () => {
    const directive = new ParallaxTwo();
    expect(directive).toBeTruthy();
  });
});
